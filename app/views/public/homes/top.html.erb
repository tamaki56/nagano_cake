<div class="container">
  <div class="row">
    <div class="mx-auto">
      <p>ようこそ,ながのケーキへ!<br>
      このサイトは、ケーキ販売のECサイトになります。<br>
      会員でない方も商品の閲覧はできますが、<br>
      購入には会員登録が必要になります。</p>
    </div>
  </div>
  <div class="row">
    <div class="col-lg-7">
      <h2>新着商品</h2>
      
      <% @items.first(4).each do |item| %>
        <%= link_to item_path(item.id) do %>
          <%= image_tag item.get_image(200,200), size:"200x200" %>
          <p><%= item.name %></p>
          <p><%= item.price %></p>
        <% end %>
      <% end %>
      <%= link_to "全ての商品を見る", items_path %>
    </div>
  </div>
</div>
